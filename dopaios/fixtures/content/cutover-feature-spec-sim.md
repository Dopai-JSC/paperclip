# Feature spec giả lập cho fixture cutover — FS-SIM-001 revision 1

Tài liệu giả lập (nhãn `test`) đóng vai feature spec được Cutover Plan pin theo
ID–revision–hash trong fixture cutover giả lập FX-05 (KC-17). Không phải feature spec
thật; không thuộc Product Baseline nào.

Phạm vi giả lập: một feature tối thiểu có đúng một work-item đầu tiên chạy trong
runtime sau cutover (`WI-SIM-001`).
